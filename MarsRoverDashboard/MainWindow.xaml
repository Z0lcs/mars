<Window x:Class="MarsRoverDashboard.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        Title="Mars Rover Telemetria Dashboard" Height="800" Width="1400" Background="#F0F2F5" WindowState="Maximized">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="20" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Row="0" Grid.Column="0" Background="White" CornerRadius="8" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Akkumulátor Szint (%)" FontWeight="Bold" FontSize="16" Foreground="#333" Margin="0,0,0,10"/>
                <lvc:CartesianChart Grid.Row="1" Series="{Binding AkkuSeries}" LegendLocation="None">
                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis Title="Eltelt idő (Óra)" LabelFormatter="{Binding XFormatter}"/>
                    </lvc:CartesianChart.AxisX>
                    <lvc:CartesianChart.AxisY>
                        <lvc:Axis Title="Akku (%)" MinValue="0" MaxValue="100"/>
                    </lvc:CartesianChart.AxisY>
                </lvc:CartesianChart>
            </Grid>
        </Border>

        <Border Grid.Row="0" Grid.Column="2" Background="White" CornerRadius="8" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Ásványgyűjtés Üteme (db)" FontWeight="Bold" FontSize="16" Foreground="#333" Margin="0,0,0,10"/>
                <lvc:CartesianChart Grid.Row="1" Series="{Binding AsvanySeries}" LegendLocation="None">
                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis Title="Eltelt idő (Óra)" LabelFormatter="{Binding XFormatter}"/>
                    </lvc:CartesianChart.AxisX>
                    <lvc:CartesianChart.AxisY>
                        <lvc:Axis Title="Összes Ásvány" MinValue="0"/>
                    </lvc:CartesianChart.AxisY>
                </lvc:CartesianChart>
            </Grid>
        </Border>

        <Border Grid.Row="0" Grid.Column="4" Background="White" CornerRadius="8" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Időfelhasználás (Taktusok)" FontWeight="Bold" FontSize="16" Foreground="#333" Margin="0,0,0,10"/>
                <lvc:PieChart Grid.Row="1" Series="{Binding StatuszSeries}" LegendLocation="Bottom" InnerRadius="40"/>
            </Grid>
        </Border>

        <Border Grid.Row="2" Grid.Column="0" Background="White" CornerRadius="8" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Sebesség-profil" FontWeight="Bold" FontSize="16" Foreground="#333" Margin="0,0,0,10"/>
                <lvc:CartesianChart Grid.Row="1" Series="{Binding SebessegSeries}" LegendLocation="None">
                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis Title="Fokozat" Labels="{Binding SebessegLabels}"/>
                    </lvc:CartesianChart.AxisX>
                    <lvc:CartesianChart.AxisY>
                        <lvc:Axis Title="Használat (alkalom)"/>
                    </lvc:CartesianChart.AxisY>
                </lvc:CartesianChart>
            </Grid>
        </Border>

        <Border Grid.Row="2" Grid.Column="2" Background="White" CornerRadius="8" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Bányászat (Nappal vs Éjjel)" FontWeight="Bold" FontSize="16" Foreground="#333" Margin="0,0,0,10"/>
                <lvc:CartesianChart Grid.Row="1" Series="{Binding NapszakSeries}" LegendLocation="Bottom">
                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis Title="Napszak" Labels="{Binding NapszakLabels}"/>
                    </lvc:CartesianChart.AxisX>
                    <lvc:CartesianChart.AxisY>
                        <lvc:Axis Title="Kibányászott (db)"/>
                    </lvc:CartesianChart.AxisY>
                </lvc:CartesianChart>
            </Grid>
        </Border>

        <Border Grid.Row="2" Grid.Column="4" Background="White" CornerRadius="8" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Küldetés Teljesítése (67 db)" FontWeight="Bold" FontSize="16" Foreground="#333" Margin="0,0,0,10" HorizontalAlignment="Center"/>
                <lvc:CartesianChart Grid.Row="1" Series="{Binding TeljesitmenySeries}" LegendLocation="Right">
                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis Labels="{Binding TeljesitmenyLabels}"/>
                    </lvc:CartesianChart.AxisX>
                    <lvc:CartesianChart.AxisY>
                        <lvc:Axis Title="Ásvány (db)" MinValue="0" MaxValue="70"/>
                    </lvc:CartesianChart.AxisY>
                </lvc:CartesianChart>
            </Grid>
        </Border>

    </Grid>
</Window>